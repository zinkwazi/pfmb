##### Configuration #####
# Hosts config file. Enter full path if using crontab. 
config=/etc/pfmb/hosts.conf

# SSH backup username
backupuser=backup

# Backup directory. Enter full path if using crontab.
backupdir=/storage/backup/machines/pfsense

# Number of backups to keep.
backupcopies=25
